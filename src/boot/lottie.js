import { boot } from 'quasar/wrappers'

// Dejamos este archivo vacío a propósito.
// El componente Vue3Lottie se importará localmente en las
// páginas que lo necesiten (como DashboardPage.vue).
// Esto evita conflictos de registro global vs local.
export default boot(() => {
  // No se necesita registrar nada globalmente.
})
